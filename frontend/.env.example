# Blank uses the current origin. Vite proxies /api to localhost:3001 in development.
VITE_API_URL=
# For separate production hosting, set the actual HTTPS API origin before building.
# VITE_API_URL=https://api.example.com
