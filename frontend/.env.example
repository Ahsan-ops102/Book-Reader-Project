# Copy to .env.local for local dev (Vite auto-loads .env.local, gitignored)

# Local dev: your backend running on your Mac
VITE_API_URL=http://localhost:3001

# When deployed: point this at your Azure VM's domain instead, e.g.
# VITE_API_URL=https://api.yourdomain.me
